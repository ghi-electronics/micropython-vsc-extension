{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "micropython",
            "request": "launch",
            "name": "MicroPython Deploy and Debug (USB)",
            "program": "${workspaceFolder}/main.py",
            "sync": true,
            "stopOnEntry": false
        }
    ]
}
